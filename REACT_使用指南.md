# ReAct 模式使用指南 🚀

## ✅ 实现完成！

Agent 现在会**真正执行操作**，不再只是描述步骤！

## 🎯 核心改进

| 之前 | 现在 |
|------|------|
| ❌ Agent：建议你重命名... | ✅ Agent：✓ 创建文件夹 Papers |
| ❌ Agent：建议你移动... | ✅ Agent：✓ 移动 paper.pdf |
| ❌ 只有描述 | ✅ 真正执行 |

## 🚀 快速开始

### 1. 确保配置正确

```bash
# 确保 .env 文件已配置
cat .env | grep CUSTOM_API
```

应该看到：
```
DEFAULT_AI_PROVIDER=custom
CUSTOM_API_BASE_URL=https://api.deepseek.com/v1
CUSTOM_API_KEY=sk-xxx
CUSTOM_API_MODEL=deepseek-chat
```

### 2. 运行测试

```bash
# 方式1：使用测试脚本（推荐首次使用）
python test_react_mode.py

# 方式2：直接使用 CLI
uv run smart-tidy agent ./test_files --request "智能整理这些文件"
```

### 3. 查看输出

现在你会看到详细的执行过程：

```
============================================================
[Agent] 迭代 1/15
============================================================

[Agent] 💭 Thought: 我需要先扫描目录了解有哪些文件

[Agent] 🔧 Action: file_scanner
[Agent] 📝 Action Input: {
  "directory": "./test_files"
}

[Agent] 📊 Observation: {"files": [...], "total": 11}

============================================================
[Agent] 迭代 2/15
============================================================

[Agent] 💭 Thought: 发现7个PDF文件，需要检查是否为论文

[Agent] 🔧 Action: file_analyzer
[Agent] 📝 Action Input: {
  "file_path": "./test_files/paper.pdf",
  "check_if_paper": true
}

[Agent] 📊 Observation: {"paper_check": {"likely_paper": true}}

... (继续执行更多操作)

[Agent] ✅ 任务完成
```

## 🎨 日志说明

- 💭 **Thought** - Agent 的思考过程
- 🔧 **Action** - 调用的工具名称
- 📝 **Action Input** - 工具参数
- 📊 **Observation** - 工具执行结果
- ✅ **完成** - 任务成功完成

## 📊 验证结果

检查 `test_files` 目录：

```bash
ls test_files/
```

应该看到：
```
Papers/                    # ← 新创建的文件夹
  paper1.pdf              # ← 已移动
  paper2.pdf              # ← 已移动
  ...
其他非论文文件（保持原位）
```

## 🔧 工作原理

### ReAct 模式

Agent 使用 "Thought → Action → Observation" 循环：

1. **Thought**: Agent 思考下一步该做什么
2. **Action**: Agent 调用具体的工具
3. **Observation**: 系统返回工具执行结果
4. **重复**: 直到任务完成

这个过程**不依赖** LLM 的 function calling 功能，所以：
- ✅ 适用于所有 LLM
- ✅ 支持自定义 API
- ✅ 无需特殊配置

## 🎯 实际使用

### 整理论文

```bash
# 整理 Downloads 文件夹的论文
uv run smart-tidy agent ~/Downloads

# 整理当前目录
uv run smart-tidy agent .

# 先预览不执行
uv run smart-tidy agent ./test_files --dry-run
```

### 其他操作

```bash
# 按主题分类
uv run smart-tidy agent ./papers --request "按研究领域分类论文"

# 规范文件名
uv run smart-tidy agent ./papers --request "整理论文并规范化文件名"
```

## 🐛 常见问题

### Q1: Agent 没有调用工具？

**A**: 检查 LLM 输出格式。确保 Agent 输出包含：
```
Thought: ...
Action: ...
Action Input: {...}
```

如果没有，可能需要：
- 增加提示词的强调
- 或调整 LLM temperature

### Q2: 工具执行失败？

**A**: 查看详细日志，Agent 会显示：
```
[Agent] ❌ 工具执行失败: <错误信息>
```

常见原因：
- 文件路径不正确
- 权限不足
- 磁盘空间不足

### Q3: 达到最大迭代次数？

**A**: 默认15次迭代，如果不够：
- 简化任务（分批处理）
- 或增加 `max_iterations` 参数

## 📚 更多文档

- **详细实现**: [REACT_MODE_IMPLEMENTATION.md](REACT_MODE_IMPLEMENTATION.md)
- **论文整理**: [论文整理使用指南.md](论文整理使用指南.md)
- **项目文档**: [README.md](README.md)

## 🎉 开始使用！

```bash
# 一键整理论文
uv run smart-tidy agent ./test_files
```

就这么简单！Agent 会自动：
1. ✅ 扫描文件
2. ✅ 识别论文
3. ✅ 创建文件夹
4. ✅ 移动论文
5. ✅ 生成报告

**现在真正能自动整理论文了！** 🎊

---

**完成时间**: 2026-01-20  
**状态**: ✅ 已实现并测试  
**支持**: 所有 LLM（包括自定义 API）
