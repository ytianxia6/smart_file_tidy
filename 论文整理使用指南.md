# 📚 论文整理使用指南

> **一条命令，自动整理所有学术论文！**

## 🚀 快速开始

### 1. 确保已配置 API

```bash
# 如果还没有配置，复制配置模板
cp env.custom.example .env

# 编辑 .env，推荐使用 DeepSeek
# DEFAULT_AI_PROVIDER=custom
# CUSTOM_API_BASE_URL=https://api.deepseek.com/v1
# CUSTOM_API_KEY=sk-your-key
# CUSTOM_API_MODEL=deepseek-chat
```

### 2. 整理论文

```bash
# 自动整理当前目录的论文
uv run smart-tidy agent ./test_files

# 或者明确说明需求
uv run smart-tidy agent ./test_files --request "智能整理这些文件"
```

**就这么简单！** Agent 会自动：
- ✅ 扫描目录中的 PDF 文件
- ✅ 识别哪些是学术论文
- ✅ 创建 "Papers" 文件夹
- ✅ 将论文移动到该文件夹

## 📖 详细使用

### 预览模式（推荐首次使用）

如果你不确定会发生什么，先用 `--dry-run` 预览：

```bash
uv run smart-tidy agent ./test_files --dry-run
```

这会显示将要执行的操作，但**不会真正移动文件**。

### 正式执行

确认预览结果后，移除 `--dry-run`：

```bash
uv run smart-tidy agent ./test_files
```

### 处理不同目录

```bash
# 整理下载文件夹
uv run smart-tidy agent ~/Downloads

# 整理文档文件夹
uv run smart-tidy agent ~/Documents

# 整理当前目录
uv run smart-tidy agent .
```

## 💡 工作原理

### Agent 执行步骤

```
1️⃣ 扫描目录
   找到所有文件，识别 PDF

2️⃣ 识别论文
   对每个 PDF 进行论文特征检测：
   - Abstract（摘要）
   - Introduction（引言）
   - References（参考文献）
   - DOI、arXiv ID 等

3️⃣ 创建文件夹
   创建 "Papers" 或 "学术论文" 文件夹

4️⃣ 移动论文
   将识别的论文移动到专门文件夹

5️⃣ 生成报告
   统计扫描、识别、移动的文件数量
```

### 论文识别标准

Agent 检查以下特征：

**高置信度（5+ 个特征）**：
- ✅ Abstract
- ✅ Introduction
- ✅ References
- ✅ Conclusion
- ✅ Keywords
- ✅ DOI
- ✅ Journal/Conference

**中等置信度（3-4 个特征）**：
- 可能是论文，但需要进一步分析

**低置信度（< 3 个特征）**：
- 可能不是学术论文

## 🎯 使用场景

### 场景1：整理下载的论文

```bash
# 每周整理一次下载文件夹
cd ~/Downloads
uv run smart-tidy agent .
```

### 场景2：批量整理历史文件

```bash
# 整理多年积累的论文
uv run smart-tidy agent ~/Documents/Old_Papers
```

### 场景3：按主题整理

```bash
# 如果需要按主题分类（高级功能）
uv run smart-tidy agent ./papers --request "按研究领域分类论文"
```

## 🛠️ 高级功能

### 1. 论文元信息提取

Agent 可以提取（如果文件包含）：
- 📝 论文标题
- 👥 作者列表
- 📅 发表年份
- 📚 期刊/会议名称
- 🔗 DOI
- 🏷️ 研究领域

### 2. 智能命名

Agent 可以根据论文信息生成规范文件名：
```
格式：作者_年份_标题.pdf
示例：Zhang_2024_Deep_Learning_Survey.pdf
```

要启用此功能：
```bash
uv run smart-tidy agent ./papers --request "整理论文并规范化文件名"
```

### 3. 按主题分类

```bash
uv run smart-tidy agent ./papers --request "按研究主题分类论文"
```

Agent 会：
- 分析论文主题
- 创建主题文件夹（如：AI、医学、物理等）
- 将论文移动到相应文件夹

## 🐛 常见问题

### Q1: Agent 只给建议，不执行操作？

**A**: 这个问题已经修复！新版本 Agent 会真正执行操作。确保你使用的是最新版本。

### Q2: 如何撤销操作？

**A**: 所有操作都有日志记录。如果需要撤销，可以查看日志并手动移回：

```bash
# 查看操作日志
cat data/logs/operations.log
```

或者使用 `--dry-run` 先预览。

### Q3: 误判怎么办？

**A**: 
1. 首先用 `--dry-run` 预览
2. 如果发现误判，可以：
   - 手动移回文件
   - 或在移动前用 `file_analyzer` 单独检查

```bash
# 单独检查一个文件
uv run smart-tidy analyze ./file.pdf
```

### Q4: 处理大量文件会很慢吗？

**A**: Agent 使用了优化的两级识别策略：
1. 快速规则检测（不调用 LLM）
2. 仅在需要时使用 LLM 深度分析

通常情况下：
- 100 个文件 ≈ 2-5 分钟
- 大部分时间花在 LLM 调用上

### Q5: 支持中文论文吗？

**A**: 完全支持！Agent 可以识别：
- 中文论文（包含"摘要"、"参考文献"等）
- 英文论文
- 中英文混合论文

## 🎨 自定义

### 修改文件夹名称

如果你想使用其他文件夹名称，可以在请求中指定：

```bash
uv run smart-tidy agent ./papers --request "将论文移动到 Academic_Papers 文件夹"
```

### 调整识别阈值

编辑 `src/langchain_integration/tools/file_analyzer_tool.py`：

```python
# 默认阈值：3 个特征
likely_paper = indicator_count >= 3

# 更严格：5 个特征
likely_paper = indicator_count >= 5

# 更宽松：2 个特征
likely_paper = indicator_count >= 2
```

## 📊 示例输出

### 成功执行

```
[Agent] 开始处理任务...
[Agent] 目录: ./test_files
[Agent] 需求: 智能整理这些文件

📚 任务类型：学术论文整理（默认模式）

⠋ Agent正在工作中...

执行结果：

✅ 整理完成！

扫描统计：
- 总文件数：11 个
- PDF 文件：7 个
- 其他文件：4 个

识别结果：
- 学术论文：7 篇
- 非论文：0 个

执行操作：
✓ 创建文件夹：./test_files/Papers
✓ 移动 FULLTEXT01.pdf -> Papers/FULLTEXT01.pdf
✓ 移动 ijoc.11.4.345.pdf -> Papers/ijoc.11.4.345.pdf
✓ 移动 k2001.pdf -> Papers/k2001.pdf
✓ 移动 RectangleBinPack.pdf -> Papers/RectangleBinPack.pdf
✓ 移动 学术写作规范和伦理随堂测试-MEM-3.pdf -> Papers/...
✓ 移动 学术写作规范和伦理随堂测试-MEM-4.pdf -> Papers/...
✓ 移动 学术写作规范和伦理随堂测试-MEM-5.pdf -> Papers/...

总结：成功移动 7 个论文到 Papers 文件夹！
```

## 🎓 最佳实践

### 1. 定期整理

建议每周运行一次：

```bash
# 添加到定时任务（Linux/Mac）
crontab -e
# 每周日晚上运行
0 20 * * 0 cd ~/Downloads && uv run smart-tidy agent .
```

### 2. 先预览再执行

```bash
# 第一次使用新目录时
uv run smart-tidy agent ./new_folder --dry-run

# 确认无误后正式执行
uv run smart-tidy agent ./new_folder
```

### 3. 备份重要文件

虽然 Agent 很可靠，但建议备份重要文件：

```bash
# 备份整个目录
cp -r ./test_files ./test_files_backup

# 或使用 git
cd ./test_files
git init
git add .
git commit -m "备份"
```

### 4. 查看日志

所有操作都有日志记录：

```bash
# 查看最近的操作
tail -n 50 data/logs/operations.log

# 查看特定日期的操作
grep "2024-01-20" data/logs/operations.log
```

## 🆘 获取帮助

### 查看帮助

```bash
uv run smart-tidy agent --help
```

### 测试导入

```bash
python test_agent_import.py
```

### 完整测试

```bash
python test_paper_organization.py
```

### 查看文档

- [论文整理实现文档](PAPER_ORGANIZATION_IMPLEMENTATION.md)
- [LangChain 集成文档](docs/LANGCHAIN_INTEGRATION.md)
- [自定义 API 配置](docs/CUSTOM_API_LANGCHAIN.md)

## 🎉 开始使用！

现在你已经了解了所有内容，开始整理你的论文吧：

```bash
# 1. 进入包含论文的目录
cd ~/Downloads

# 2. 先预览
uv run smart-tidy agent . --dry-run

# 3. 确认后执行
uv run smart-tidy agent .

# 4. 检查结果
ls -la Papers/
```

就这么简单！🎊

---

**祝你的论文管理井井有条！** 📚✨
