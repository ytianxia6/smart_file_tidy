# Smart-Tidy 命令对比指南 📖

## 🎯 三种整理模式

Smart-Tidy 提供三种文件整理模式，适合不同场景：

| 命令 | 后端技术 | 执行方式 | 交互性 | 推荐场景 |
|------|---------|---------|--------|---------|
| `agent` | LangChain Agent | 自动执行 | 单次确认 | **一次性任务** |
| `interactive` | LangChain Agent | 自动执行 | 多轮交互 | **探索性整理** |
| `organize` | AI Adapter | 预览+手动 | 需要确认 | **谨慎模式** |

## 📋 详细对比

### 1. Agent 模式 ⚡ (推荐)

```bash
uv run smart-tidy agent ./test_files --request "整理这些论文"
```

**特点**:
- ✅ **自动执行**: Agent 自动分析 → 自动操作 → 完成
- ✅ **智能决策**: 使用 ReAct 模式，Agent 自主决策
- ✅ **真实操作**: 真正创建文件夹、移动文件
- ✅ **快速高效**: 一次性完成任务
- ❌ **单次运行**: 执行完就退出，不能继续调整

**适用场景**:
- 明确的整理需求
- 批量处理任务
- 自动化脚本
- CI/CD 流程

**示例**:
```bash
# 整理论文
uv run smart-tidy agent ./downloads --request "识别学术论文并分类"

# 按类型整理
uv run smart-tidy agent ./documents --request "按文件类型整理"

# 清理重复文件
uv run smart-tidy agent ./photos --request "删除重复的图片"
```

---

### 2. Interactive 模式 🔄 (探索推荐)

```bash
uv run smart-tidy interactive ./test_files
```

**特点**:
- ✅ **自动执行**: 与 agent 模式相同的后端
- ✅ **多轮交互**: 可以连续多次整理
- ✅ **灵活调整**: 根据结果继续优化
- ✅ **智能决策**: 同样使用 ReAct 模式
- ✅ **反馈循环**: 可以根据结果继续调整

**适用场景**:
- 不确定如何整理
- 需要多次尝试
- 逐步优化结果
- 学习和探索

**交互流程**:
```
1. 扫描文件
2. 输入需求
3. Agent 执行 → 显示结果
4. 是否继续？
   - 是 → 返回步骤 2
   - 否 → 退出
```

**示例对话**:
```
请描述您的整理需求: 整理论文
✓ 完成！移动了 4 个论文到 Papers/

是否继续整理？ [Y/n]: y

请描述您的整理需求: 再整理一下图片
✓ 完成！移动了 3 个图片到 Images/

是否继续整理？ [Y/n]: n
退出
```

---

### 3. Organize 模式 🛡️ (保守模式)

```bash
uv run smart-tidy organize ./test_files --request "整理文件"
```

**特点**:
- ❌ **手动执行**: 先预览，手动确认后执行
- ❌ **传统AI**: 使用旧的 AI adapter，不是 Agent
- ✅ **操作预览**: 详细的操作表格
- ✅ **安全保守**: 不会自动执行
- ✅ **备份支持**: 可选创建备份

**适用场景**:
- 重要文件，需要谨慎
- 想先看操作预览
- 不信任自动执行
- 学习文件操作逻辑

**流程**:
```
1. 扫描文件
2. AI 分析 → 生成操作计划
3. 显示操作预览表格
   ┌────┬──────┬─────────┬─────────┬──────────┐
   │ 序号│ 操作 │  文件   │  目标   │   原因   │
   ├────┼──────┼─────────┼─────────┼──────────┤
   │ 1  │ move │ a.pdf   │ Papers/ │ 学术论文 │
   └────┴──────┴─────────┴─────────┴──────────┘
4. 是否执行？
   - 是 → 执行操作
   - 否 → 取消
```

---

## 🎯 如何选择？

### 决策树

```
开始
  │
  ├─ 明确知道要做什么？
  │   ├─ 是 → 一次性完成？
  │   │   ├─ 是 → 使用 agent ⚡
  │   │   └─ 否 → 使用 interactive 🔄
  │   └─ 否 → 使用 interactive 🔄
  │
  └─ 需要预览操作？
      └─ 是 → 使用 organize 🛡️
```

### 场景推荐

| 场景 | 推荐命令 | 原因 |
|------|---------|------|
| 整理下载文件夹的论文 | `agent` | 明确任务，一次完成 |
| 不知道如何整理一堆文件 | `interactive` | 多次尝试，逐步优化 |
| 整理重要的工作文档 | `organize` | 需要预览，谨慎操作 |
| 自动化脚本定期整理 | `agent` | 无需交互，直接执行 |
| 清理临时文件 | `agent --dry-run` | 先模拟看看 |
| 学习如何整理文件 | `interactive` | 交互式学习 |

---

## 💡 最佳实践

### 1. 首次使用
```bash
# 先用 dry-run 模式试试
uv run smart-tidy agent ./test_files --request "整理" --dry-run

# 确认没问题后，正式执行
uv run smart-tidy agent ./test_files --request "整理"
```

### 2. 复杂整理
```bash
# 使用 interactive 分步整理
uv run smart-tidy interactive ./documents

# 第一轮：整理论文
# 第二轮：整理图片
# 第三轮：整理其他文件
```

### 3. 批量处理
```bash
# 使用 agent 批量处理多个目录
uv run smart-tidy agent ./folder1 --request "整理"
uv run smart-tidy agent ./folder2 --request "整理"
uv run smart-tidy agent ./folder3 --request "整理"
```

### 4. 安全第一
```bash
# 重要文件用 organize 模式
uv run smart-tidy organize ./important --request "整理" --no-backup false

# 会先显示预览，再手动确认
```

---

## 🚀 快速参考

### Agent 模式
```bash
# 基本使用
uv run smart-tidy agent <目录> --request "<需求>"

# 常用参数
--dry-run          # 仅模拟，不实际执行
--provider custom  # 指定 AI 提供商
```

### Interactive 模式
```bash
# 基本使用（启动后会提示输入需求）
uv run smart-tidy interactive <目录>

# 常用参数
--recursive        # 递归扫描子目录
--provider custom  # 指定 AI 提供商
```

### Organize 模式
```bash
# 基本使用
uv run smart-tidy organize <目录> --request "<需求>"

# 常用参数
--dry-run         # 仅预览
--recursive       # 递归扫描
--no-backup       # 不创建备份
--batch-size 50   # 批处理大小
```

---

## 🔧 其他实用命令

### 查看历史
```bash
uv run smart-tidy history --limit 20
```

### 撤销操作
```bash
uv run smart-tidy undo
```

### 分析文件
```bash
uv run smart-tidy analyze ./file.pdf
```

### 获取建议
```bash
uv run smart-tidy suggest ./documents
```

---

## ❓ 常见问题

### Q: Agent 和 Interactive 有什么区别？
**A**: 
- **Agent**: 单次执行，执行完退出
- **Interactive**: 多轮交互，可以连续整理

### Q: 为什么 Interactive 不显示操作预览？
**A**: Interactive 使用 Agent 自动执行，不需要预览。如果需要预览，使用 `organize` 命令。

### Q: 如何确保不会误操作？
**A**: 
1. 先用 `--dry-run` 模式
2. 或使用 `organize` 模式，会显示预览
3. 或备份重要文件

### Q: 三种模式哪个最智能？
**A**: `agent` 和 `interactive` 同样智能（都用 Agent），`organize` 使用传统 AI。

### Q: 我应该用哪个？
**A**: 
- 90% 的情况用 `agent` ⚡
- 需要多次尝试用 `interactive` 🔄
- 需要谨慎用 `organize` 🛡️

---

**修订时间**: 2026-01-26
**版本**: 2.0
**状态**: ✅ Interactive 模式已修复，三种模式均可正常工作
