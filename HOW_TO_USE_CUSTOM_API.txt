===============================================
智能文件整理助手 - 自定义API使用指南
===============================================

您已配置 DEFAULT_AI_PROVIDER=custom

以下是完整的使用步骤：

===============================================
第1步：配置 .env 文件
===============================================

在项目根目录创建或编辑 .env 文件：

Windows:
  copy env.custom.example .env
  notepad .env

Linux/Mac:
  cp env.custom.example .env
  nano .env

填写以下内容（以DeepSeek为例）：

DEFAULT_AI_PROVIDER=custom
CUSTOM_API_BASE_URL=https://api.deepseek.com/v1
CUSTOM_API_KEY=sk-your-deepseek-api-key
CUSTOM_API_MODEL=deepseek-chat

===============================================
第2步：验证配置
===============================================

运行测试脚本：

  python examples/test_custom_api.py

如果看到 "🎉 所有测试通过！"，说明配置成功！

如果测试失败，请检查：
  ✓ .env 文件是否在项目根目录
  ✓ API密钥是否正确
  ✓ API地址是否包含 /v1 后缀
  ✓ 网络连接是否正常

===============================================
第3步：开始使用
===============================================

Agent模式（推荐）：

  # 智能整理文件
  smart-tidy agent ./test_files --request "按文件类型分类"

  # 获取整理建议（不执行操作）
  smart-tidy suggest ./test_files

  # 分析单个文件
  smart-tidy analyze ./test_files/file.pdf

  # 与AI助手对话
  smart-tidy chat

传统模式：

  # 交互式整理
  smart-tidy interactive ./test_files

  # 单次整理
  smart-tidy organize ./test_files --request "整理PDF文件"

===============================================
常用命令
===============================================

# 预览操作（不实际执行）
smart-tidy agent ./directory --request "整理文件" --dry-run

# 查看帮助
smart-tidy --help
smart-tidy agent --help

# 查看操作历史
smart-tidy history

# 撤销操作
smart-tidy undo

===============================================
获取API密钥
===============================================

DeepSeek（推荐）：
  1. 访问 https://platform.deepseek.com/
  2. 注册并登录
  3. 创建API密钥
  4. 复制到 .env 文件

通义千问：
  1. 访问 https://dashscope.console.aliyun.com/
  2. 登录阿里云账号
  3. 开通DashScope服务
  4. 创建API密钥
  5. 复制到 .env 文件

Moonshot AI：
  1. 访问 https://platform.moonshot.cn/
  2. 注册并登录
  3. 创建API密钥
  4. 复制到 .env 文件

===============================================
故障排除
===============================================

问题1: "自定义API地址未配置"
解决: 检查 .env 文件中的 CUSTOM_API_BASE_URL

问题2: "API密钥未配置"
解决: 检查 .env 文件中的 CUSTOM_API_KEY

问题3: 连接超时
解决: 
  - 检查网络连接
  - 确认API地址正确
  - 尝试其他服务

问题4: 测试失败
解决: 运行 python examples/test_custom_api.py 查看详细错误

===============================================
详细文档
===============================================

快速开始:
  CUSTOM_API_QUICKSTART.md

详细配置:
  docs/CUSTOM_API_LANGCHAIN.md

Agent使用:
  docs/LANGCHAIN_INTEGRATION.md

配置模板:
  env.custom.example

===============================================
示例：整理下载文件夹
===============================================

# 1. 先获取建议
smart-tidy suggest ~/Downloads

# 2. 使用Agent整理
smart-tidy agent ~/Downloads --request "按文件类型分类，清理临时文件"

# 3. 如果不满意，可以撤销
smart-tidy undo

===============================================
成本估算（以DeepSeek为例）
===============================================

整理100个文件：
  - 输入: 约200K tokens
  - 输出: 约50K tokens
  - 成本: 约¥0.20-0.30

整理1000个文件：
  - 成本: 约¥2-3

DeepSeek价格:
  - 输入: ¥1/百万tokens
  - 输出: ¥2/百万tokens

===============================================
需要帮助？
===============================================

1. 查看文档: docs/CUSTOM_API_LANGCHAIN.md
2. 运行测试: python examples/test_custom_api.py
3. 提交Issue: GitHub仓库

===============================================
